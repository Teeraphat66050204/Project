---
type Props = {
  class?: string;
  as?: "article" | "section" | "div";
};

const { class: className = "", as = "div" } = Astro.props;
const Tag = as;
---

<Tag class:list={["card-premium rounded-2xl border border-white/10 bg-[#1F2937]", className]}>
  <slot />
</Tag>
